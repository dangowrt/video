config kodi 'main'
	# Where Kodi keeps userdata, add-ons, databases and thumbnails. It
	# grows to hundreds of megabytes, so it belongs on its own volume;
	# give fstab a mount for this path and the service waits for it.
	option data_dir '/srv/kodi'
	option temp_dir '/tmp/kodi'
