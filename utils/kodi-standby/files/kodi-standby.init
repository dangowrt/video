#!/bin/sh /etc/rc.common

START=95
USE_PROCD=1
PROG=/usr/sbin/kodi-standbyd

start_service() {
	procd_open_instance
	procd_set_param command "$PROG"
	procd_set_param respawn 3600 5 0
	procd_set_param stderr 1
	procd_close_instance
}
